# Keep JSON snapshot goldens LF-only so byte comparisons in the tests that
# read them are stable regardless of a contributor's core.autocrlf.
internal/tools/testdata/params_schema.json text eol=lf
cmd/brunel/testdata/list_files_response.json text eol=lf
