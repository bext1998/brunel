# Keep the tools parameter-schema snapshot LF-only so the byte comparison in
# schema_snapshot_test.go is stable regardless of a contributor's core.autocrlf.
internal/tools/testdata/params_schema.json text eol=lf
